"""Shared plumbing reused by every example and study.

Keeping the frame math (``frames``), the inverse-kinematics + plotting helpers (``ik``)
and the pyorerun visualization boilerplate (``viz``) here is what lets the per-dataset
model definitions stay short and non-duplicated.
"""
