"""Clinical dataset: upper-limb model + progressive examples.

Marker convention (see ``examples/data/testFlorent_clinicalData.c3d``):
SJN, CV7, SXS, TV8 (thorax), RSAA, RSIA, RSRS, RCAJ + Cluster_RS_0x (scapula),
RGJC, RHME, RHLE + Cluster_RA_0x (humerus). Anatomical landmarks and skin-cluster
markers can be toggled as IK inputs via ``marker_set``.
"""
